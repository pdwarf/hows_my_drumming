<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= t('app_name') %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-full bg-gray-100">
    <% unless content_for?(:hide_navbar) %>  
      <header class="bg-white shadow-md no-print">
        <nav class="container mx-auto px-4 py-4 flex items-center justify-between">
          <% if user_signed_in? %>
            <%= link_to drum_sessions_path, class: "flex items-center" do %>
              <%= image_tag "drumfeedback-com_logo2x_text-only.png", alt: t('app_name'), class: "h-14 w-auto" %>
            <% end %>
          <% else %>
            <%= link_to root_path, class: "flex items-center" do %>
              <%= image_tag "drumfeedback-com_logo2x_text-only.png", alt: t('app_name'), class: "h-10 md:h-14 w-auto" %>
            <% end %>
          <% end %>
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to t('layout.my_sessions'), drum_sessions_path, class: "text-gray-600 hover:text-blue-600" %>
              <%= link_to t('layout.qr_code'), qr_code_path, class: "text-gray-600 hover:text-blue-600", target: "_blank", rel: "noopener noreferrer" %>
              <%= button_to t('layout.sign_out'), destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded" %>
            <% else %>
              <%= link_to t('layout.sign_in'), new_user_session_path, class: "outline outline-2 text-blue-500 px-4 py-2 rounded" %>
            <% end %>
          </div>
        </nav>
      </header>
    <% end %>

    <main class="flex-grow container mx-auto px-4 py-8">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-white py-4 mt-auto">
      <div class="container mx-auto px-4 text-center text-gray-500">
      <%= t('layout.footer', year: Time.current.year) %>
      </div>
    </footer>
  </body>
</html>